# Промпт для агента новой сессии — Этап 2 (кэш по входу + resume)

Скопируй всё ниже разделителя в первое сообщение новой сессии Claude Code,
запущенной из `~/Projects/video-pipeline`.

---

Ты подключаешься к платному контракту по стабилизации чужого проекта
`video-pipeline` (конвейер генерации видеороликов). Я — исполнитель, ты — мой
инженер. Этапы 0-1 и код этапа 5 сданы предыдущими сессиями — не переделывай.

## ПЕРВЫЙ ХОД — новая ветка

Ты сейчас в `stage-5-llm-contracts`. До любой работы создай от неё новую
ветку и работай только в ней:

    git checkout -b stage-2-cache-resume

В main/чужие ветки не пушить. `WORK_PLAN.md` и `AGENT_ONBOARDING_*.md`
не коммитить — внутренние документы исполнителя.

## Что уже сделано (прочитай в этом порядке)

1. `docs/openspec/project.md` — карта capability, порядок этапов 1→5→2→4→3.
2. `docs/openspec/system-map.md` — карта системы (25 агентов, транспорт,
   §3 воркер/startup_guard, §6-8 лизинги/реконсайлеры — твой материал).
3. `docs/openspec/specs/cache-resume/spec.md` — ТВОЯ спека на этот этап.
4. `docs/openspec/changes/stage-5-llm-contracts/` — что сделал этап 5:
   реестр контрактов `app/contracts/` (схемы = нормативный «вход» для
   input_hash), политика `run_with_contract`, метрики
   `data/<proj>/llm_metrics.jsonl`.
5. `WORK_PLAN.md` (корень, не в git) — смета, этап 2.

## Контекст от этапа 5 (важно для твоего дизайна)

- Выход агента типизирован контрактом (`app/contracts/__init__._REGISTRY`,
  12 схем) — input_hash строй от: вход агента + имя+версия схемы контракта
  + версия промпта + модель + параметры. Порядок этапов «5 до 2» был выбран
  ровно ради этого — не изобретай второй нормативный вход.
- Транспорт: `chat(response_schema=…)`, режим `GPT_STRUCTURED_OUTPUTS`
  (см. `app/settings.py`), дев-релей — Chattiq (`chattiq.ru`).
- Хвосты этапа 5, НЕ твоя работа, но не сломай: приёмочные живые прогоны
  ещё не сделаны (ждут `.env`); kie/vibecode не пробованы (деградация);
  маркеры `*_degraded_no_llm` в project.meta.

## Объём этапа 2 (WORK_PLAN, критерии — в спеке)

A. `input_hash` (вход + версия промпта + модель + параметры) в NodeRun;
   готовые места — NodeRun.meta/attempts (`models.py:755-816`; Attempt —
   МЁРТВАЯ таблица, не строй на ней, карта §7).
B. Персистентный курсор прогресса шага (не только running/ready) —
   образцы уже есть: чекпоинты img_pr (`img_pr_batches.save_checkpoint`),
   chunk-чекпоинты scene_design, `ai_jobs` в project.meta.
C. Инвалидация чекпоинтов при смене входа (лечит «протухшие результаты»
   после смены промпта); правило спеки: кэшируются только ПОЛНЫЕ принятые
   результаты (частичный батч N-1 из commit-per-batch — не кэш-хит).
D. Lease с TTL/owner на единицу работы. Известные лизинги без TTL (карта
   §6): img_gen_inflight, video_gen_inflight, video_gen_skip,
   vision_check_passed, _SPLIT_LOCK (in-process). Единственный образец
   с TTL — montage_lane (`montage_coexist.py:17-59`, 7200 c).

Известные грабли (карта §3, §9): startup_guard откатывает все running к
step.requires («рестарт = откат назад» — ровно то, что чинишь); второй
legacy-воркер `app/worker.py` со своим ACTIVE_STATUSES (§9 #2) — двойное
исполнение; коллизии ord в `_STATUS_ORDER` (§9 #3); 4 точки поднятия
воркера (§3).

## Порядок работ (SDD)

1. Создай openspec-change `stage-2-cache-resume` в `docs/openspec/changes/`:
   proposal.md → tasks.md → дельта-спека. Прогони через скилл plan-review
   (панель чужих моделей), закрой блокеры. Покажи мне proposal ДО кода.
2. Врезайся минимально: `NodeRun.meta`, существующие чекпоинты,
   `startup_guard`, `run_sync` — не переписывай оркестратор.
3. Одна задача за раз; перед кодом — короткий план врезки, жди ок
   (если я сказал «газуй» — едешь блоками, коммит на блок).

## Правила (те же, что раньше)

- Секреты в чат не выводить, работать по именам переменных; `.env` закрыт
  пермишенами — команды с ним даю я через `!`.
- Тесты: `.venv/bin/python -m pytest tests/ -q` (Python 3.12, venv стоит).
  Базовый уровень среды: 69 предсуществующих фейлов (нет gitignored-промптов
  и .env) — сверяй СПИСОК фейлов с чистым деревом, не только счётчик;
  тестовый прогон пересобирает ai-pack/ в рабочем дереве — `git restore
  ai-pack/` после, НЕ коммитить (там жил утёкший ключ).
- Замеряй, не заявляй; зелёные тесты ≠ приёмка (harness-гейт в тестах
  выключен). Приёмка этапа — живой прогон: убитый на ~80% прогон доезжает
  рестартом, платя только за остаток; смена промпта пересчитывает ровно
  конус зависимостей.

**Первый ход:** `git checkout -b stage-2-cache-resume`, потом прочитай
5 документов из списка и покажи мне план proposal.
