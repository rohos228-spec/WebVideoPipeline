# Промпт для агента новой сессии — Этап 3 (учёт стоимости llm_calls)

Скопируй всё ниже разделителя в первое сообщение новой сессии Claude Code,
запущенной из `~/Projects/video-pipeline`.

---

Ты подключаешься к платному контракту по стабилизации чужого проекта
`video-pipeline` (конвейер генерации видеороликов). Я — исполнитель, ты — мой
инженер. Этапы 0-1, код этапов 5, 2 и 4 сданы предыдущими сессиями — не
переделывай. Порядок исполнения: 1 → 5 → 2 → 4 → **3 (ты, последний)**.

## ПЕРВЫЙ ХОД — новая ветка

Ты сейчас в `stage-4-checks-cascade`. До любой работы создай от неё новую
ветку и работай только в ней:

    git checkout -b stage-3-cost-accounting

В main/чужие ветки не пушить. `WORK_PLAN.md` и `AGENT_ONBOARDING_*.md`
не коммитить — внутренние документы исполнителя.

## Порядок работ (обязательный цикл, как у этапов 2 и 4)

1. **Ветка** (выше) → снять baseline тестов ДО первой правки кода.
2. **План**: openspec-change `stage-3-cost-accounting` в
   `docs/openspec/changes/` (proposal.md → tasks.md → дельта-спека).
3. **Ревью плана**: скилл `/plan-review` (панель чужих моделей) по
   proposal+tasks+дельте с контекстом target-спеки и карты; блокеры
   закрыть правками плана, маркеры [панель N/M] в текст; коммит доков.
4. **Кодинг**: блоками, коммит на блок, после блока — прогон затронутых
   тестов; сверка полного прогона со СПИСКОМ baseline-фейлов.
5. **Код-ревью**: субагент `code-critic` по всему диффу ветки; каждую
   находку проверить по коду; подтверждённые починить, тесты на
   починенные баги, финальный полный прогон = baseline.

## Что уже сделано (прочитай в этом порядке)

1. `docs/openspec/project.md` — карта capability; твоя — `cost-accounting`.
2. `docs/openspec/system-map.md` — §4.1 (транспорт, слои ретраев — ТВОЙ
   основной материал), §9 находка 4 (Attempt — мёртвая таблица, учёт на
   ней НЕ строить).
3. `docs/openspec/specs/cost-accounting/spec.md` — ТВОЯ спека, в ней уже
   зафиксированы решения: самописный учёт (НЕ Langfuse), строка на
   фактический HTTP-вызов, logical_call_id, unbilled, contract_rejected,
   один дашборд UI в смете.
4. `docs/openspec/changes/stage-5-llm-contracts/` — контракты и
   repair-политика (колбэк «contract_rejected» стыкуется с ней).
5. `docs/openspec/changes/stage-2-cache-resume/` + tasks («Отклонения
   реализации» в конце).
6. `docs/openspec/changes/stage-4-checks-cascade/` — свежесданный этап 4
   (тоже с «Отклонениями реализации» в конце tasks.md).
7. `WORK_PLAN.md` (корень, не в git) — смета, этап 3.

## Контекст от этапов 5/2/4 (важно для твоего дизайна — НЕ дублируй)

- **prompt_version_hash уже существует**: `app/services/input_hash.py:70`
  (этап 2, сделан общим для этапа 3 сознательно) — sha256 эффективного
  текста промпта + вшитые хинты. НЕ изобретай второй — импортируй.
- **llm_metrics.jsonl** (этап 5, `data/<proj>/llm_metrics.jsonl` +
  `scripts/llm_repair_rate.py`) — счётчики repair-политики. Это ДРУГОЙ
  учёт (единицы работы политики, не HTTP-вызовы и не деньги); не сливай
  и не ломай, но исход `contract_rejected` для llm_calls бери колбэком
  из той же политики (`app/contracts/policy.py`).
- **Механика паузы с причиной готова** (этап 4): `meta["pause_reason"]`
  ({code, ...}) + ветка в `notify_step_done` (`app/telegram/bot.py`) +
  операторские решения. Бюджет-предохранитель делай тем же паттерном
  (code=`budget_exhausted`), не выдумывай второй канал.
- **contextvar project/node/model** уже есть — `app/services/llm_override.py`
  (`bind_project_llm` в обвязке шага); хук учёта берёт контекст оттуда,
  41 call-site не трогается.
- Незакрытые хвосты этапов 5/2/4 (НЕ твоя работа): живые приёмочные
  прогоны ждут `.env`; kie/vibecode не пробованы; порог
  VISION_CHECK_MAX_ROUNDS калибруется заказчиком.

## Объём этапа 3 (WORK_PLAN, критерии — в спеке)

A. Один хук учёта в `gpt_api.chat()` (карта: `gpt_api.py:2052`; строки
   могли уехать — сверь) — каждый фактический HTTP-вызов, включая ретраи,
   доборы, continuation и НЕуспешные; связка `logical_call_id`.
B. Починить суммирование usage на склейках: `usage=first.usage` — сейчас
   минимум `gpt_api.py:2035` и `:2151` (+ проверь merge :1837-1846 и
   volume-добор) → sum по частям.
C. Таблица `llm_calls` (в проектной SQLite, `Base.metadata.create_all`,
   как work_leases этапа 2) + прайс-таблица моделей в репо (неизвестная
   модель = цена 0 + warning) + запись НЕуспешных (tokens=NULL + флаг
   unbilled при обрыве без тела).
D. `prompt_version_hash` в каждую запись (импорт из input_hash) +
   `contract_rejected` колбэком из repair-политики.
E. Бюджет-предохранитель на прогон (default из конфига; превышение →
   paused с причиной, паттерн этапа 4) + дашборд в студии: нода →
   вызовы/токены/$/неуспешные, сумма прогона, разбивка по моделям
   (ОДИН дашборд — смета; аналитика сверх — роадмап).

Известные грабли: вложенные ретраи 5 слоёв не знают друг о друге (§3
аудита) — хук ставится в САМОМ нижнем HTTP-слое, не в обёртках; таблица
`Attempt` мёртвая — не использовать (§9#4); стоимость логического
вызова/ноды — всегда агрегат SUM, отдельных строк-сумм нет (спека);
next.js-студия — вне сметы везде, кроме одного дашборда E.

## Правила (те же, что раньше)

- Секреты в чат не выводить, работать по именам переменных; `.env` закрыт
  пермишенами — команды с ним даю я через `!`.
- Тесты: `.venv/bin/python -m pytest tests/ -q` (Python 3.12, venv стоит).
  Базовый уровень среды: 69 предсуществующих фейлов — сверяй СПИСОК
  фейлов с чистым деревом (сними baseline ДО первой правки), не только
  счётчик; тестовый прогон пересобирает ai-pack/ в рабочем дереве —
  `git restore ai-pack/` после, НЕ коммитить (там жил утёкший ключ).
- Тесты, фиксирующие старое (чинимое) поведение, обновляй с комментарием
  «Этап 3: …» — как делали этапы 5/2/4.
- Замеряй, не заявляй; зелёные тесты ≠ приёмка (harness-гейт в тестах
  выключен). Приёмка этапа — живой прогон: стоимость ролика по агентам
  видна в UI, включая неуспешные вызовы; цифры сходятся с биллингом
  релея по строкам с usage (unbilled — количеством).

**Первый ход:** `git checkout -b stage-3-cost-accounting`, потом прочитай
7 документов из списка, сними baseline тестов и покажи мне план proposal.
Если я скажу «газуй» — едешь всем циклом сам: план → /plan-review →
код блоками (коммит на блок) → code-critic → финальная сверка с baseline.
