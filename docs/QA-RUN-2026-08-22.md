# QA LIVE — первый живой прогон на MiniMax

**Старт:** 2026-08-22 13:30
**Ветка:** `hardening` (от `c8312ba`)
**Цель:** довести ПЕРВЫЙ ролик до финального mp4 (9:16, 60–75 с).
**Провайдеры:** текст/картинки/видео — MiniMax, озвучка — ElevenLabs HTTP API.
**Падение:** фикс кода → рестарт бэкенда → повтор того же шага.

## Окружение

| Поле | Значение |
|------|----------|
| ruff check | 0 |
| ruff format | 3 файла были не отформатированы → починено |
| mypy | 0 (317 файлов) |
| pytest_ratchet | новых падений нет (56 известных), 9 позеленело из-за локальных `prompts/` — базу НЕ трогал |
| check_prompts | «ядро на месте» |
| ffmpeg/ffprobe | /usr/bin, есть |
| Studio UI | собран (`web/out`, next export) |
| auto_mode | 0 |

## Журнал

| Время | Делаю | Сделал | Результат | Баг/фикс |
|-------|-------|--------|-----------|----------|
| 13:30 | ШАГ 0 | ruff/mypy/ратчет/промты | всё зелёное, кроме format | D0 FIX |
| 13:35 | ревизия маршрута медиа | грep вызовов `minimax.generate_video` | вызовов нет вовсе | D1 FIX in code |
| 13:50 | тесты на маршрут | 2 новых теста в `test_outsee_retry.py` | зелёные | — |
| 13:42 | шаг 1 «Сценарий», запуск №1 | POST /steps/plan/run | висело 3 мин, потом `VIBECODE_API_KEY пуст` | D5, D6 |
| 13:46 | диагностика зависания | `python3 -m asyncio pstree <pid>` (3.14) | задача стояла в `work_lease.release` → aiosqlite | D6 |
| 13:50 | шаг 1, запуск №2 | после правок D5/D6 | `text_llm.chat → MiniMax (MiniMax-M3)`, оверрайда ноды нет | — |
| 13:53 | шаг 1 «Сценарий» | 2:48 на вызов, ответ 3008 симв. | `plan_ready`, общий план 2934 симв., 70 с / ≈980 знаков закадра — по формату | учёт вызова не записался: D9 |
| 13:57 | шаг 2 «Закадровый текст» | 2:51 на вызов | `script_ready`, 976 знаков — попадание в целевые 980 | — |
| 13:58 | шаг 3 «Разбивка» | 2:20 на вызов | `frames_ready`, 12 кадров, закадр по кадрам 959 симв. | очередь дозаписи учёта отработала |
| 14:01 | шаг «Объекты» | POST /steps/objects/run | 400: «нода для шага objects не найдена в WorkflowRun» — в графе ноды `objects` нет, следующая по рёбрам `n_excel_gpt_1` | D10 |
| 14:01 | enrich 1 | POST /steps/enrich/run?node_key=n_excel_gpt_1 | 1:07 на вызов, apply-ops JSON с первой попытки (ops=12, repairs=0) — план Б MiniMax работает | — |
| 14:03 | шаг «Объекты» (hero) | POST с `?node_key=n_hero` | `hero skipped (hero_count=0)` и **бесконечный цикл**: статус не сохранялся | D11 FIX |
| 14:06 | повтор после правки D11 | тот же шаг | `hero_ready` с первого раза, цикла нет | — |
| 14:06 | items | POST /steps/items/run?node_key=n_items | `items_ready` за 3 с (предметов нет) | — |
| 14:07 | enrich 2, попытка через `enrich` | POST /steps/enrich/run?node_key=n_excel_gpt_2 | ушёл повторный прогон **слота 1** — `node_key` кодом `enrich` игнорируется | D12 |
| 14:09 | enrich 2 через `excel_gpt` | POST /steps/excel_gpt/run?node_key=n_excel_gpt_2 | `enriching_2` — слот берётся из `slotIndex` ноды | — |
| 14:11–14:15 | enrich 2, контракт | 3 вызова, 2 repair-ретрая, 5 минут | прошло с третьей попытки; отбивало поле-комментарий при валидных 12 ops | D13 FIX |
| 14:15 | промты картинок | POST /steps/img_pr/run?node_key=n_image_prompts | — | — |

## Дефекты

| ID | Где | Ожидание | Факт | Sev |
|----|-----|----------|------|-----|
| D0 | `media_ledger.py`, `test_minimax.py`, `test_elevenlabs_api.py` | `ruff format --check` зелёный | 3 файла не отформатированы (коммит `c8312ba` не прогонялся через гейт) | P3, пофикшено |
| D1 | `services/outsee_retry.py:generate_video_with_retries` | `VIDEO_PROVIDER=minimax` → клип идёт в MiniMax | ветки minimax в диспетчере не было: `minimax.generate_video` не вызывался ниоткуда, лестница уезжала в браузерный Outsee CDP | P0, пофикшено |
| D2 | `bots/minimax.py` | одна ступень разрешения на запрос и на учёт | `_generate_video_inner` клэмпил в {768P,1080P}, `generate_video` — в {512P,768P,1080P}: запрос 512P уходил как 1080P, а в `media_calls` писалась цена 512P | P1, пофикшено (`normalize_video_resolution`) |
| D3 | `bots/minimax.py` | длительность из набора MiniMax (6/10) | лестница шлёт `duration or 5`, 5 уезжало в API как есть | P2, пофикшено (`normalize_video_duration`) |
| D4 | `bots/minimax.py` | studio-id `hailuo-2-3-fast` → `MiniMax-Hailuo-2.3-Fast` | не совпадало с именем модели → молчаливый откат на дефолт (в 1.7× дороже) | P2, пофикшено (алиасы) |
| D5 | `services/vibecode_catalog.py:resolve_node_choice` | нет канвас-ноды → провайдер из `.env` (`TEXT_LLM_PROVIDER=minimax`) | подставлялся каталожный дефолт `gpt-5.6-sol` с провайдером vibecode и перебивал `.env`: шаг падал на `VIBECODE_API_KEY пуст`. Те же дефолты прибиты к outsee для картинок (`gpt-image-2-vip`) и видео (`veo-3-1-lite`) — то есть шаги 6 и 9 упали бы так же | P0, пофикшено |
| D6 | `services/work_lease.py` + `orchestrator/pipeline.py` | освобождение lease в `finally` — мгновенное | `release()` открывал ВТОРОЕ соединение к SQLite, пока сам `advance_project` держал транзакцию: самозахват, `busy_timeout` 60 с × 3 ретрая = 3 минуты на каждом падении шага, потом `database is locked ×3` | P0, пофикшено (release сессией вызывающего) |
| D7 | `services/work_lease.py:acquire` | осиротевший lease своего процесса перехватывается | owner привязан к asyncio-задаче, каждый такт воркера — новая задача; `expire_dead_local_leases` свой pid пропускает → после сбоя release проект стоял намертво **час** (TTL шага), воркер молотил «занят живым lease» каждые 5 с | P0, пофикшено (`_reclaim_local_orphan` + реестр живых owner'ов) |
| D8 | `generation_options.py` | MiniMax можно выбрать в мастере | в каталоге картинок MiniMax не было вовсе — любой выбор уезжал в outsee/grsai | P2, добавлен `minimax_image_01` |
| D9 | `services/llm_ledger.py`, `services/media_ledger.py` | строка учёта доезжает до БД | `INSERT llm_calls упал — database is locked`: шаг целиком идёт в одной транзакции (`advance_project_job`), SQLite пускает одного writer'а, поэтому посторонний INSERT отбивается ровно во время работы. `/llm-costs` показывал `calls: 0` при потраченных $0.023 | P1, пофикшено (очередь + дозапись после закрытия транзакции). Первая строка ($0.023 за шаг 1) потеряна до правки |
| D10 | шаг `objects` / каталог шагов | `POST /steps/objects/run` работает на дефолтном графе | в дефолтном workflow ноды типа `objects` нет (есть `hero` и `items`), запуск отбивается 400. Каталог `/steps/catalog` при этом шаг показывает — то есть UI предлагает кнопку, которой нет ноды | P2, не блокирует: следующая нода по рёбрам запускается явно через `?node_key=` |
| D11 | `services/storage_step_sync.py` | статус, выставленный шагом, доезжает до БД | `session.refresh(project)` в начале `sync_storage_after_step` выбрасывал несохранённые правки: ветка «герои не нужны» ставила `hero_ready` без flush, и статус откатывался к `generating_hero`. Воркер запускал шаг заново каждые 5 с — **бесконечный цикл**, проект не двигается | P0, пофикшено (flush перед refresh) |
| D12 | `/api/projects/steps/catalog` + `start_step` | код шага из каталога уважает `node_key` | код `enrich` из каталога всегда гонит слот 1: `node_key` учитывается только для кода `excel_gpt`, которого в каталоге нет. Через API это тихий повторный прогон уже сделанного слота (лишние деньги), в Studio не видно, потому что UI шлёт `excel_gpt` | P3 |
| D13 | `contracts/apply_ops.py` | комментарий модели не отменяет пакет операций | `extra="forbid"` на конверте: MiniMax дважды подряд подписал валидный ответ полем `report`/`target`, и 16 КБ разобранной работы отбрасывались целиком. Три вызова и пять минут на шаг вместо одного. Structured outputs у MiniMax нет — промт единственная дисциплина | P1, пофикшено (болтовня выбрасывается, операции под чужим именем по-прежнему ошибка) |
