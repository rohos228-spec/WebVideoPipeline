# Задачи — батч-порт E6

- [x] A.1 `main.py`: гейт running-шага.
- [x] A.2 `node_groups`: canvas_has + needs_upgrade + _forget; GET-guard.
- [x] A.3 Play resume: `project_steps` auto_mode + `projects.py` mode/resolved + 3 tsx.
- [x] A.4 `run_sync` inactive heal + `gpt_api` Cancelled + `scene_design` ×2.
- [x] A.5 Пропуски зафиксированы (dual-DB, drop_*, b0eb7106).
- [x] B.1 `tsc --noEmit` чист.
- [x] C.1 4 теста (2 новых файла, 2 в существующие); 70/70 в связанных модулях.
- [x] C.2 Гейт: ruff/mypy/policy чисто (`main.py:350` — предсущее).
- [ ] C.3 Отчет владельцу. Без коммита до команды.
