# Задачи — батч-порт E1+E2

- [x] A.1 `camera_expand`: split/dangling/balance + 4 теста.
- [x] A.2 `vo_shot`: 15 сущностей + resolve/inherit/apply правки + 11 тестов.
- [x] A.3 `db_frames_context` + `img_pr_batches` + `xlsx_runners` (4-tuple).
- [x] A.4 `prompt_library` маркеры + 2 шаблона.
- [x] A.5 `planner` overflow BFS + тест; `safe_upload` + тест.
- [x] A.6 Пропуски зафиксированы (fw-цепочка, camera-menu, PNG-родитель, QC-валидаторы).
- [x] C.1 113/113 в связанных модулях; 2 предсущих падения зафиксированы.
- [x] C.2 Гейт: ruff/mypy/policy чисто.
- [ ] C.3 Отчет владельцу. Без коммита до команды.
