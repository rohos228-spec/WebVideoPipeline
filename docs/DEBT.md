# Реестр долга

Единое место, где записано всё осознанно ослабленное. Правила —
`docs/ENGINEERING-POLICY.md` §12:

- исключение заводится только вместе со строкой здесь;
- число исключений может только уменьшаться, рост — красный
  `policy-check` (база: `.claude/baselines/debt.json`);
- долг старше 90 дней требует решения владельца: гасить или признать
  постоянным с обоснованием.

Погасил долг — убери исключение из гейта, вычеркни строку отсюда и
обнови базу: `python3 scripts/policy_check.py --update-debt`.

## Активный долг

| # | Что ослаблено | Где | Почему | Заведено | Чем гасится |
|---|---|---|---|---|---|
| 1 | Тесты на изменённые строки перенесённого кода заказчика: 25 файлов, ~790 строк | `--exclude` в diff-cov (`.claude/verify.json`) | Решение владельца 2026-09-04: фронт заказчика нужен на проде сразу, тесты на его код — отдельная работа | 2026-09-04 | Таблица файлов и приоритет — `docs/openspec/changes/merge-customer-fork/proposal.md` §8; задачи — там же `tasks.md` блок I |
| 2 | Литералы цвета в каталогах студии заказчика | `grep -v -e …` в hex-гейте (`.github/workflows/ci.yml`) | Дизайн заказчика перенесён как есть (183 литерала); чинить у себя — сделать следующий перенос невозможным | 2026-09-03 | Только вместе с решением заказчика о переходе на токены |
| 3 | Отпечатки gitleaks: демо-ключ кадра, имена kling-моделей | `.gitleaksignore` | Доказанные ложные срабатывания на строках вида `api_model: "kling-…"` | 2026-09-03 | Не гасится: это не секреты. Строка нужна, чтобы число отпечатков не росло молча |
| 4 | Плавающее покрытие `app/services/anim_pr_sidecar.py` (96.9–97.7) | планка в `.claude/baselines/coverage.json` по нижнему значению | Покрытие сайдкара зависит от таймингов прогона; планка по пику красит следующий прогон | 2026-09-04 | Детерминированный тест на ветки сайдкара вместо тайминга |
| 5 | Группа палитры «Сценарий → промпты кадров + QC» скрыта | `app/services/node_groups.py` | Ноды группы исполняются цепочкой заказчика (`enrich_xlsx`/`fw_frames`), которая не переносилась | 2026-09-03 | Решение заказчика, чья цепочка основная (отдельный change) |
| 6 | Нет суточного потолка трат на медиа-генерацию | `app/services/llm_ledger.py` считает только LLM | Бюджет заводился под LLM-контур (спека stage-3); картинки и видео идут мимо учёта | 2026-09-04 | Учёт стоимости медиа в том же леджере + пауза проекта при превышении |
| 7 | Нет метрик: доля неуспешных нод, расход за сутки, возраст последнего ролика | наблюдаемость (политика §15) | Инцидент разбирается по логам вручную | 2026-09-04 | Минимальный `/api/metrics` или выгрузка в лог по расписанию |
| 8 | Нет машинной проверки на входе кода из форка заказчика | политика §5, WR-6 | Заказчик работает без хуков; 25 исключений diff-cov приехали именно оттуда | 2026-09-04 | Скрипт сверки форка перед переносом (состав, секреты, гейт-конфиг) |

## Погашено

Пока пусто. Строка переезжает сюда с датой и коммитом, которым закрыта.
