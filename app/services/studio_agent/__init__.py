"""Агент студии: разговор поверх конвейера, а не вместо него.

Пользователь пишет в чат, чего хочет; агент ведёт его по шагам, показывая
цену до нажатия и результат после (`docs/SAAS-PIVOT.md` §8). Здесь живёт
серверная половина: набор инструментов и петля разговора.

**Жёсткое правило, ради которого всё и разложено так** (§8.2): агент —
дирижёр, а не автор конвейера. Он вызывает существующие шаги в порядке,
который задан конечным автоматом оркестратора, и не решает, что делать
дальше. Обоснование в `docs/SHOTS-FIX-2026-08-23.md` §11: пять дефектов
подряд имели одну форму — требование адресовали модели и не проверили, что
оно доехало. Дать LLM право строить конвейер значит воспроизвести этот класс
дефектов на верхнем уровне, где он дороже.

Технически это значит: инструмент `runStep` спрашивает у графа, разрешён ли
шаг сейчас, и отказывает, если нет. Модель может попросить что угодно —
выполнится только допустимое.
"""

from app.services.studio_agent.tools import TOOLS, ToolError, call_tool, tool_manifest

__all__ = ["TOOLS", "ToolError", "call_tool", "tool_manifest"]
