# Промпт для агента новой сессии — Этап 4 (петля обратной связи проверок)

Скопируй всё ниже разделителя в первое сообщение новой сессии Claude Code,
запущенной из `~/Projects/video-pipeline`.

---

Ты подключаешься к платному контракту по стабилизации чужого проекта
`video-pipeline` (конвейер генерации видеороликов). Я — исполнитель, ты — мой
инженер. Этапы 0-1, код этапов 5 и 2 сданы предыдущими сессиями — не
переделывай. Порядок исполнения этапов: 1 → 5 → 2 → **4 (ты)** → 3.

## ПЕРВЫЙ ХОД — новая ветка

Ты сейчас в `stage-2-cache-resume`. До любой работы создай от неё новую
ветку и работай только в ней:

    git checkout -b stage-4-checks-cascade

В main/чужие ветки не пушить. `WORK_PLAN.md` и `AGENT_ONBOARDING_*.md`
не коммитить — внутренние документы исполнителя.

## Что уже сделано (прочитай в этом порядке)

1. `docs/openspec/project.md` — карта capability; твоя — `checks-cascade`
   (Req 1 внедрён этапом 0, Req 2-8 — твой target).
2. `docs/openspec/system-map.md` — карта системы; §5 (4 контура проверок,
   vision_check_loop, «[ok] навсегда», media_probe, STYLE-lock) — ТВОЙ
   основной материал; §9 находки 5-11, 19 — адресованы этапу 4.
3. `docs/openspec/specs/checks-cascade/spec.md` — ТВОЯ спека.
4. `docs/openspec/changes/stage-5-llm-contracts/` — контракты: реестр
   `app/contracts/` (в т.ч. `CheckReport`/vp.check.v1), политика
   `run_with_contract`, C.5: битый отчёт проверки = repair-retry, НЕ
   `verdict: fail` (строгий парсер — опт-ин параметром).
5. `docs/openspec/changes/stage-2-cache-resume/` — proposal + tasks
   (включая раздел «Отклонения реализации» в конце tasks.md).
6. `WORK_PLAN.md` (корень, не в git) — смета, этап 4.

## Контекст от этапов 5 и 2 (важно для твоего дизайна)

- Этап 2 уже дал тебе куски инфраструктуры — НЕ дублируй:
  `drop_vision_passed_for_frame` (`vision_check_loop.py`) — инвалидация
  кадра снимает его [ok]-токены; `Frame.attrs["img_input_hash"/"video_input_hash"]`
  — привязка PNG/mp4 к входу; `STEP_DEPENDENCIES` + каскад `reset_step`
  по data-DAG (возвраты vision — НЕ data-рёбра, не добавляй их в DAG);
  протухшие медиа переносятся в `scenes|videos/stale/`.
- Hash кадра считается от АКТУАЛЬНОГО `Frame.image_prompt` — твои правки
  промпта вердиктом (db_patch/[VISION_FIX]) автоматически инвалидируют
  старый PNG. Это задумано, опирайся.
- Отчёты проверок типизированы `CheckReport`; не изобретай второй парсер —
  расширяй контракт и `check_analysis` поверх него.
- Незакрытые хвосты этапов 5/2 (НЕ твоя работа, не сломай): живые
  приёмочные прогоны ждут `.env`; kie/vibecode не пробованы; code-critic
  ревью этапа 2 мог оставить фикс-коммиты в ветке — прими их как данность.

## Объём этапа 4 (WORK_PLAN, критерии — в спеке)

A. Лимит регенераций из конфига (default 2, сейчас 20 —
   `vision_check_loop.py:39`); исчерпание = pause С ПРИЧИНОЙ (сейчас
   тихий return False, счёт со следующего захода с нуля — §5.1, №11).
B. Вердикт всех 9 осей → промпт регенерации (сейчас доезжают 2 из 10:
   character/clones — `vision_regen_fix.py`; авто-фикс только shot1 и
   только scenes, hero/videos — никогда — №7; ось logic отсутствует в
   шаблоне scores, overall плавает — `check_analysis.py:235-245` №8).
C. Каскад дёшево→дорого: `media_probe` (чёрный кадр/длительность/aspect/
   тишина) ПЕРЕД vision. Точки врезки — §5.3 карты: `enrich_xlsx.py:735`,
   `:756-765` (mp4→стиллы), `video_sheet.py:64` (дубль probe с fallback
   8.0 с — убрать), `generate_videos`/`generate_images` приёмка файлов.
D. Убрать «навсегда [ok]» до разрешения гейта: [ok] фиксируется даже из
   отчёта с verdict:fail (`vision_check_loop.py:526`); неупомянутый
   кадр = принят (`mark_passed_except_regen`); гейт К1 перебивает свежий
   fail старым pass (`:515-523` №5); severity-only отчёт без scores
   проходит гейт автоматом (`check_analysis.py:820-824` №9).

Известные грабли: 4 несогласованных контура проверок (§5, карта);
промпты К2/К3 под .gitignore — на чистом клоне не работают (№19);
`auto_review.review_image` мёртв; regex номеров кадров из critical —
голым `\b(\d{1,4})\b` → ложные regen-цели (`check_analysis.py:875-921`);
`check_analysis.py` — 1587 строк и 52 регулярки, переработка парсинга —
РОАДМАП, не твой этап: врезайся точечно.

## Порядок работ (SDD)

1. Создай openspec-change `stage-4-checks-cascade` в
   `docs/openspec/changes/`: proposal.md → tasks.md → дельта-спека.
   Прогони через скилл plan-review (панель чужих моделей), закрой
   блокеры. Покажи мне proposal ДО кода.
2. Врезайся минимально: `vision_check_loop`, `check_analysis`,
   `vision_regen_fix`, `media_probe` — контуры К2/К3 и xlsx-слой не
   переписывай.
3. Одна задача за раз; перед кодом — короткий план врезки, жди ок
   (если я сказал «газуй» — едешь блоками, коммит на блок; после
   каждого блока — прогон затронутых тестов).

## Правила (те же, что раньше)

- Секреты в чат не выводить, работать по именам переменных; `.env` закрыт
  пермишенами — команды с ним даю я через `!`.
- Тесты: `.venv/bin/python -m pytest tests/ -q` (Python 3.12, venv стоит).
  Базовый уровень среды: 69 предсуществующих фейлов — сверяй СПИСОК
  фейлов с чистым деревом (сними baseline ДО первой правки), не только
  счётчик; тестовый прогон пересобирает ai-pack/ в рабочем дереве —
  `git restore ai-pack/` после, НЕ коммитить (там жил утёкший ключ).
- Тесты, фиксирующие старое (чинимое) поведение, обновляй с комментарием
  «Этап 4: …» — как делали этапы 5/2.
- Замеряй, не заявляй; зелёные тесты ≠ приёмка (harness-гейт в тестах
  выключен). Приёмка этапа — живой прогон: регенерация сходится за ≤2
  круга либо паузит с причиной; дешёвые probe-проверки отсекают брак
  (битый mp4, чёрный кадр) ДО платного LLM-vision.

**Первый ход:** `git checkout -b stage-4-checks-cascade`, потом прочитай
6 документов из списка и покажи мне план proposal.
